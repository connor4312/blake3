[package]
name = "blake3-js"
version = "0.1.0"
edition = "2018"

[lib]
name = "thread_count"
crate-type = ["cdylib"]

[build-dependencies]
neon-build = "0.3.3"

[dependencies]
blake3 = { version = "0.2", features = ["c"] }
neon = "0.3"
